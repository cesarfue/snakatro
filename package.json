{
  "name": "snakatro_elec",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build && electron .",
    "build": "rm -rf dist && mkdir dist && tsc -p tsconfig.json && tsc -p tsconfig.preload.json && cp index.html dist/ && cp -r src/shaders dist/src/",
    "watch": "tsc -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.14.0",
    "electron": "^35.1.5",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/electron": "^1.6.10"
  },
  "type": "module"
}
